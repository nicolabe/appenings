if($('#newAppening').is(":visible")){
	$(".appening_form_container").html("");
	$("#newAppening").html("<%= escape_javascript( render partial: "form" ) %>");
	$("#new_appening_field").focus();
} else {
	//it's visible, do nothing
}
